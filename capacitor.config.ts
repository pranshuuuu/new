import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krypton.android',
  appName: 'krypton',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
